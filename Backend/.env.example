# Archivo de configuración de variables de entorno para desarrollo MANUAL
# NOTA: Para desarrollo con Docker, usa el archivo .env.docker en el directorio raíz
# 
# INSTRUCCIONES:
# 1. Copia este archivo: cp .env.example .env
# 2. Modifica los valores según tu configuración local
# 3. Ejecuta: npm install && npm run dev

# Configuración del Servidor
PORT=3000

# Configuración de la Base de Datos (para desarrollo manual)
DB_USER=sa
DB_PASSWORD=tu_contraseña_sql_server
DB_SERVER=localhost
DB_DATABASE=ToDoDB

# Configuración JWT (¡IMPORTANTE! Cambia esta clave en producción)
JWT_SECRET=tu_clave_secreta_muy_segura_aqui

# Configuración de Entorno
NODE_ENV=development

# Configuración de Email (Opcional - para funcionalidades futuras)
EMAIL_USER=tu_email@dominio.com
EMAIL_PASS=tu_contraseña_de_aplicacion
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
