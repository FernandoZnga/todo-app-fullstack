# Configuración para Docker Compose
# Este archivo contiene las variables de entorno para el entorno Docker

# Configuración del Servidor
PORT=3000

# Configuración de la Base de Datos
# Para Docker: DB_SERVER=sqlserver (sobreescrito en docker-compose.yml)
# Para desarrollo manual: DB_SERVER=localhost
DB_USER=sa
DB_PASSWORD=TodoApp2024!
DB_SERVER=localhost
DB_DATABASE=ToDoDB

# Configuración JWT
JWT_SECRET=mi_clave_secreta_super_segura_2024

# Configuración de Email (Opcional - para características futuras)
EMAIL_USER=tu_email@dominio.com
EMAIL_PASS=tu_contraseña_app
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587

# Configuración de Entorno
NODE_ENV=development

# Configuración del Frontend (Vite)
# Para Docker: VITE_API_URL=http://localhost:3000/api (sobreescrito en docker-compose.yml)
# Para desarrollo manual: VITE_API_URL=http://localhost:3000/api
VITE_API_URL=http://localhost:3000/api
